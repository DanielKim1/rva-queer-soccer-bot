packages:
  - name: rva-queer-soccer-bot
    functions:
      - name: rva-queer-soccer-bot
        binary: false
        runtime: python:default
        web: false
        triggers:
          - name: rva-queer-soccer-bot
            sourceType: scheduler
            sourceDetails:
              cron: "30 23 * * 2,6"
        environment:
          TOKEN: "${TOKEN}"
